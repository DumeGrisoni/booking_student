// eslint-disable-next-line react/prop-types
const Text = ({ children }) => {
  return (
    <div className="flex justify-center items-center text-justif text-default text-primary-var-1">
      {children}
    </div>
  );
};

export default Text;
